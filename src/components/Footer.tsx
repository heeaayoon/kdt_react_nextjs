export default async function Footer() {
  return (
    <footer className="w-full min-h-20 
                            flex justify-center items-center bg-blue-400 text-white">
    k-digital 2025 2기 FrontEnd
    </footer>
  );
}

// login ? <h1></h1> : "로그인 컴포넌트" : <Login/> 


// login ? // if (login == True) => login ?

// <h1></h1>  // { 로그인 성공!}

// : "로그인 컴포넌트" // else {로그인 실패! }

// : <Login/> // else {다시?}



// if (login == True) {
//     로그인 성공!
// } else {
//     로그인 실패!
// } else {
//     다시?
// }
